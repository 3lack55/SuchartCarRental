DB_NAME=car_company
DB_PASSWORD=changeme

# ใช้เฉพาะตอน build docker-compose.prod.yml (frontend build args)
# ต้องตรงกับ origin ที่ browser จะเรียกเข้ามาจริง (เช่น https://<device>.<tailnet>.ts.net)
VITE_API_HOST=localhost
VITE_API_PORT=8080
VITE_API_PROTOCOL=http
